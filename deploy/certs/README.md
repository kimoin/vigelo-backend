# TLS certificates for private-network VNMS (optional)
#
# If VNMS serves HTTPS with a private CA, copy the CA PEM here as ca.pem
# and set in deploy/.env:
#   VNMS_TLS_CA=/certs/ca.pem
#
# HTTP over UpCloud private network (recommended to start) needs no files here.
